import WebTemplate from "@/components/WebTemplates";
import Layout from "@/layouts/Layout";

const WebTemplates = () => {
  return (
    <Layout>
      <WebTemplate />
    </Layout>
  );
};

export default WebTemplates;
